import React from "react";

import { SpinnerIcon } from "../Icons/Spinner";

const Spinner = () => {
    return (
        <>
            <SpinnerIcon />
        </>
    );
};

export default Spinner;
